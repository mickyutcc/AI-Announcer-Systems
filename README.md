# MuseGenx1000-Refactored

Quick start:
1. cp .env.example .env && fill your keys
2. pip install -r requirements.txt
3. brew install ffmpeg (Mac) / sudo apt install ffmpeg (Linux)
4. Run: python3 app.py
