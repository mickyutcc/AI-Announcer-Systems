version: "3.8"
services:
  redis:
    image: redis:6-alpine
    container_name: dev_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  clamav:
    image: mkodockx/docker-clamav:alpine
    container_name: dev_clamav
    ports:
      - "3310:3310"
    restart: unless-stopped
